/** Context usage percentage that changes the meter to a warning state. */
export const CONTEXT_WARNING_THRESHOLD = 75;

/** Context usage percentage that changes the meter to a critical state. */
export const CONTEXT_CRITICAL_THRESHOLD = 90;
